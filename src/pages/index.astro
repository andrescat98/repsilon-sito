---
import Layout from '../layouts/Layout.astro';
import { releases, events } from '../data/data';
---

<Layout title="Home">
    <!-- HERO -->
    <section class="relative min-h-screen flex items-center justify-center overflow-hidden">
        <!-- Sfondo animato — sostituisci con il tuo file video/gif/canvas -->

        <div class="fixed inset-0 z-0" id="bg-animated">
            <img src="/images/bg.jpg" alt="" class="w-full h-full object-cover" />
        </div>

        <!-- Overlay scuro -->
        <div class="absolute inset-0 z-10 bg-black/50"></div>

        <!-- Contenuto hero -->
        <div class="relative z-20 text-center px-6" id="hero-content">
            <h1 class="text-[clamp(5rem,20vw,18rem)] leading-none text-white opacity-0 animate-fadein" style="animation-delay: 0.5s;">REPSILON</h1>
            <div class="w-24 h-px bg-[#e8ff00] mx-auto mt-8 opacity-0 animate-fadein" style="animation-delay: 0.8s;"></div>
        </div>

        <!-- Scroll indicator -->
        <div class="absolute bottom-10 left-1/2 -translate-x-1/2 z-20 flex flex-col items-center gap-2 opacity-0 animate-fadein" style="animation-delay: 1.2s;">
            <span class="text-xs tracking-widest text-white/50 uppercase">Scorri</span>
            <div class="w-px h-12 bg-linear-to-b from-white/50 to-transparent animate-bounce-slow"></div>
        </div>
    </section>

    <!-- PROSSIMI EVENTI -->
    <section class="w-full bg-black/80 relative z-10" style="padding-left: 3%; padding-right: 3%; padding-top: 2rem; padding-bottom: 1rem;">
        <div class="flex items-end justify-between mb-16">
            <h2 class="text-6xl md:text-8xl text-white">LIVE</h2>
            <div class="w-32 h-px bg-[#e8ff00] mb-3"></div>
        </div>

        <div class="flex flex-col gap-0">
            {
                events.map((event, i) => (
                    <a
                        href={event.link}
                        target="_blank"
                        class="group flex items-center justify-between py-7 border-t border-white/10 hover:border-[#e8ff00]/50 transition-all duration-300 hover:px-4"
                    >
                        <div class="flex items-center gap-8">
                            <span class="text-[#e8ff00] text-sm font-mono tracking-widest w-28">{event.date}</span>
                            <span class="text-white text-xl md:text-2xl font-light group-hover:text-[#e8ff00] transition-colors">{event.venue}</span>
                        </div>
                        <div class="flex items-center gap-6">
                            <span class="text-white/50 text-sm tracking-wider uppercase">{event.city}</span>
                            <span class="text-[#e8ff00] text-xs tracking-widest uppercase">→ Info e biglietti</span>
                        </div>
                    </a>
                ))
            }
            <div class="border-t border-white/10"></div>
        </div>
    </section>

    <!-- PROSSIME USCITE -->
    <section class="w-full bg-black/80 relative z-10" style="padding-left: 3%; padding-right: 3%; padding-top: 3rem; padding-bottom: 1rem;">
        <div class="flex items-end justify-between mb-16">
            <h2 class="text-6xl md:text-8xl text-white">USCITE</h2>
            <div class="w-32 h-px bg-[#e8ff00]" style="margin-bottom: 1.2rem;"></div>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-15" style="padding: 3% 15%;">
            {
                releases.map((release) => (
                    <div class="group relative overflow-hidden bg-zinc-900 aspect-square cursor-pointer" onclick={`window.location.href='${release.page}'`}>
                        <div class="absolute inset-0 bg-linear-to-br from-zinc-800 to-black flex items-center justify-center">
                            <img
                                src={release.cover}
                                alt={release.title}
                                class="w-full h-full object-cover opacity-60 group-hover:opacity-80 group-hover:scale-105 transition-all duration-700"
                                onerror="this.style.display='none'"
                            />
                        </div>
                        <div class="absolute inset-0 bg-linear-to-t from-black via-black/20 to-transparent" />
                        <div class="absolute bottom-0 left-0 right-0 p-8">
                            <p class="text-[#e8ff00] text-xs tracking-[0.3em] uppercase mb-2">
                                {release.type} — {release.date}
                            </p>
                            <h3 class="text-white text-3xl md:text-4xl" style="font-family:'Bebas Neue',sans-serif;">
                                {release.title}
                            </h3>
                        </div>
                    </div>
                ))
            }
        </div>
    </section>
</Layout>

<style>
    @keyframes fadein {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .animate-fadein {
        animation: fadein 0.8s ease forwards;
    }

    @keyframes bounce-slow {
        0%,
        100% {
            transform: scaleY(1);
        }
        50% {
            transform: scaleY(1.3);
        }
    }

    .animate-bounce-slow {
        animation: bounce-slow 2s ease-in-out infinite;
    }
</style>
